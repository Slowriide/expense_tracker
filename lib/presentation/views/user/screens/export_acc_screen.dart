import 'package:flutter/material.dart';

class ExportAccScreen extends StatelessWidget {
  const ExportAccScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Center(child: Text('Settings Screen')));
  }
}
